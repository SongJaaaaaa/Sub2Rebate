# 当前项目状态

> 这个文件用于快速恢复上下文。每次开发完成后必须更新。

## 1. 当前阶段

阶段 0：需求与规范。

当前正在建立项目规范、文档体系和模块边界。

## 2. 已确定技术栈

```text
后端：Laravel 13
管理后台：Filament 5
用户前端：Vue 3 + Vite
UI 框架：Element Plus
状态管理：Pinia
路由：Vue Router
请求库：Axios
数据库：PostgreSQL
缓存/队列：Redis
部署：Docker + Nginx
```

## 3. 已确定第一版目标

第一版完成：

- 邀请关系（无限级金字塔）。
- 邀请树。
- 新人累计充值里程碑奖励（默认 2 次）。
- 衰减系数多级返利（无限级）。
- 独立返利余额。
- 人工提现。
- 管理后台配置。
- 完整流水。

## 4. 第一版暂不实现

- 支付宝自动充值。
- 支付宝自动提现。
- 贡献系数自动奖惩（只记录数据）。
- 活动多规则并行。
- 推广员等级。

## 5. 已确认决策

- 管理员在 Sub2API 手动加额度：触发返利（需监控 Sub2API 额度变化）。
- Sub2Rebate 自身充值/加额度：触发返利。
- 用户认证：复用 Sub2API 用户表，Sub2Rebate 签发自己的 token，区分管理员和普通用户。
- 里程碑奖励次数：默认 2 次。
- 多级分发模式：衰减系数模式（默认衰减系数 0.4，可配置），无限级金字塔结构。
- 返利池比例：默认 15%，可配置。

## 6. 当前待确认

- 提现最低金额是多少。

## 7. 下一个建议动作

创建实际项目骨架：

```text
backend/    Laravel 后端
frontend/   Vue 3 用户前端
docs/       项目文档
```

然后先实现后端基础模型和迁移，再做前端用户中心。

