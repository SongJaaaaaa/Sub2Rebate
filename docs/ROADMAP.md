# 项目路线图

## 阶段 0：需求与规范

目标：

- 明确项目技术栈。
- 明确目录结构。
- 明确文档规范。
- 明确第一版范围。

状态：进行中。

交付：

- 项目规范文档。
- 架构文档。
- 模块文档。
- 开发流程。
- 项目状态记录。

## 阶段 1：MVP 业务闭环

目标：

完成最小可用的返利闭环。

范围：

- 用户与邀请关系。
- 邀请路径树。
- 新人里程碑奖励。
- 后续多级返利。
- 独立返利余额。
- 人工提现。
- 管理后台配置。
- 完整流水。
- 余额调整合并至用户管理页面（弹窗形式），移除独立页面，避免与 API 额度充值功能冲突。
- 推荐关系页面交互增强：支持移动画布、搜索指定用户查看推荐关系、点击节点查看用户信息、大层级性能优化（虚拟渲染/懒加载子树）。
- 推广用户端推荐关系页面：只能查看自己及下级关系，上级不可见。
- 返利配置中心所有配置项增加 Tooltip 提示说明（鼠标悬停显示），说明内容参考根目录方案研讨.md 第 26 节。
- 邀请树失效节点处理：中间节点被删除、失联或防躺平判定为返利失效时，不自动上浮下级，树结构保持原样，管理端置灰展示。
- 账号登录状态与返利资格拆分：失效节点默认仍可登录，但不再获得新的里程碑或多级返利。
- 返利配置中心新增“失效节点返利处理方式”：支持“归平台”和“排除失效节点后按衰减系数重算”，并提供 Tooltip 说明。
- 轻量防躺平：连续 7 天无充值、账户余额无减少、无新增下级，则置灰并暂停返利资格；有充值后恢复。

不包含：

- 支付宝自动充值（移至阶段 2）。
- 支付宝自动提现。
- 贡献系数自动生效；第一版只先完成返利资格字段、轻量防躺平和失效节点处理口径。
- 活动返利。

验收：

- 能模拟用户充值并正确发放返利。
- 能查看返利流水。
- 能申请提现。
- 管理员能审核提现。
- 余额调整在用户管理页面弹窗中完成，不存在独立余额调整页面入口。
- 管理后台推荐关系页面可搜索指定用户、移动画布、点击节点查看详情。
- 推广用户端可查看自己下级推荐关系，无法查看上级信息。
- 返利配置每个字段都有 Tooltip 说明。
- A -> B -> C 场景中，B 返利失效后 C 仍保留在 B 下级，管理端 B 置灰。
- B 返利失效后，B 不再获得新返利；多级返利按配置决定 B 对应金额归平台或排除后重算。
- 防躺平任务能将连续 7 天无活跃用户置灰，充值后自动恢复。

## 阶段 2：支付与提现自动化

目标：

让充值和提现逐步自动化。

范围：

- 支付宝扫码充值（推广员/用户端扫码支付增加 API 额度）。
- 支付宝支付回调（回调验签 → 判断支付成功 → 自动增加额度 → 触发返利事件）。
- 支付记录。
- 对账记录。
- 支付宝提现或半自动打款。

验收：

- 推广员可通过扫码支付增加额度。
- 支付成功后能自动记录充值并触发返利。
- 支付回调不会重复处理（幂等）。
- 提现审核后能进入打款流程。

## 阶段 3：运营增强

目标：

支持大规模推广。

范围：

- 贡献系数。
- 防躺平机制。
- 历史贡献保护。
- 不活跃用户自动判定返利资格失效。
- 失效后恢复规则和人工复核流程。
- 活动返利配置。
- 推广排行榜。
- 数据统计。

验收：

- 活跃推广员能获得更高权重。
- 长期不活跃用户返利降低。
- 返利总额仍受归一化控制。

## 阶段 4：风控与财务完善

目标：

降低刷量和财务风险。

范围：

- 异常邀请检测。
- 小号识别。
- 黑名单。
- 冻结期。
- 导出对账。
- 管理员操作审计。

验收：

- 可识别异常邀请行为。
- 财务可按天/月导出对账。
- 所有敏感操作可追溯。

